{
  "name": "dk-dk-website",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test": "vitest run",
    "check:links": "node tools/check-links.mjs",
    "check:meta": "node tools/check-meta.mjs",
    "check:css": "node tools/check-css.mjs",
    "validate": "npm run build && npm run check:links && npm run check:meta && npm run check:css"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "astro": "^7.2.4",
    "tailwindcss": "^4.0.0"
  },
  "devDependencies": {
    "playwright": "^1.62.1",
    "vitest": "^4.1.11"
  }
}
